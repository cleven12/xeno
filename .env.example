# Frontend (Vite) — optional; defaults are fine for local dev
# PORT=5173
# BASE_PATH=/

# API server
PORT=5000
NODE_ENV=development

# Database (Drizzle / Postgres)
DATABASE_URL=postgresql://user:password@localhost:5432/xenohuru
